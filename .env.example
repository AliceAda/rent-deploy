# ===== 高德地图 JS API 2.0（前端找房地图）=====
# 申请：https://lbs.amap.com/  → 创建「Web端(JS API)」类型应用
# 重要：高德 2.0 需要「key」+「安全密钥(securityJsCode)」成对使用
#   - Key 名称：anju（截图里那一列）
#   - 安全密钥：开放平台「安全密钥」列（截图里显示 — 的那一列，需自行查看）
#
# 把本文件复制为 .env.local 并填入真实值；.env.local 已被 .gitignore 忽略，永不入库。
# 注意：改完 .env.local 后需【重启 dev 服务器】才能生效（Vite 启动时读取）。
VITE_AMAP_KEY=
VITE_AMAP_SECURITY_CODE=
